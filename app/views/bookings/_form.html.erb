<%= simple_form_for [car, booking] do |f| %>
  <%= f.input :start_date, as: :date, html5: true %>
  <%= f.input :end_date, as: :date, html5: true %>
  <%= f.submit "Add booking", class: "w-100 btn btn-primary rounded" %>
<% end %>
